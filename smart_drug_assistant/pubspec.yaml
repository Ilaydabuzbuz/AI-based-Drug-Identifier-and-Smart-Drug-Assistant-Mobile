name: smart_drug_assistant
description: "AI based drug identifier and smart drug assistant"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  cupertino_icons: ^1.0.8

  # Camera & Gallery (Pill Identification)
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
